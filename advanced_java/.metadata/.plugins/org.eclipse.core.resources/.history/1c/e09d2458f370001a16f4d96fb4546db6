package com.wsy.step_one.chapter11;

import java.util.Optional;

/**
 * 	创建线程组
 * @author Administrator
 *
 */
public class CreateThreadGroup {

	//ThreadGroup have two Constructors.
	public static void main(String[] args) {
		
		//1.use name
		
		//2.use parent，name
		
		Optional.of("Current ThreadGroup name "+Thread.currentThread().getThreadGroup())
			.ifPresent(System.out::println);
		Optional.of("Current Thread name "+Thread.currentThread().getThreadGroup())
		.ifPresent(System.out::println);
	}
}
